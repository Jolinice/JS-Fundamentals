#!/usr/bin/node

// Get the first and second arguments after node and the script name
const first = process.argv[2];
const second = process.argv[3];

// Print in the format: "first is second"
console.log(`${first} is ${second}`);
